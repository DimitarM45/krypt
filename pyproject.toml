[project]
name = "krypt"
version = "0.1.0"
description = "An end-to-end encrypted messaging application."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cryptography>=46.0.3",
    "pylint>=4.0.4",
    "websockets>=15.0.1",
]
